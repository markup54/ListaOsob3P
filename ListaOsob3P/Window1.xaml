<Window x:Class="ListaOsob3P.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListaOsob3P"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <DockPanel>
        <Button DockPanel.Dock="Bottom">1</Button>
        <Button DockPanel.Dock="Right">2</Button>
        <Button DockPanel.Dock="Bottom">3</Button>
        <Button DockPanel.Dock="Right">4</Button>
   
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
            <UniformGrid Grid.Column="1" Rows="2" Columns="3">
                <Button>5</Button>
                <Button>6</Button>
                <Button>7</Button>
                <Button>8</Button>
            </UniformGrid>
            <StackPanel Orientation="Horizontal">
                <Button>9</Button>
                <Button>11</Button>
                <Button>12</Button>
            </StackPanel>
            <Button Margin="10" Grid.Row="1" Grid.ColumnSpan="2">13</Button>
    </Grid>
    </DockPanel>
</Window>
